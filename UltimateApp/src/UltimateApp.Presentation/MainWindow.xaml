<Window
    x:Class="UltimateApp.Presentation.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UltimateApp.Presentation"
    xmlns:vm="using:UltimateApp.Presentation.ViewModels"
    Title="UltimateApp v5">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Nagłówek -->
        <Border Grid.Row="0" Background="{ThemeResource SystemAccentColor}" Padding="16,12">
            <StackPanel Orientation="Horizontal" Spacing="12">
                <TextBlock Text="UltimateApp v5" FontSize="22" FontWeight="Bold"
                           Foreground="White" VerticalAlignment="Center"/>
                <TextBlock Text="Platforma AI" FontSize="14" Foreground="White"
                           VerticalAlignment="Center" Opacity="0.8"/>
            </StackPanel>
        </Border>

        <!-- Siatka modułów -->
        <ScrollViewer Grid.Row="1" Padding="16">
            <ItemsRepeater x:Name="ModulesRepeater"
                           ItemsSource="{x:Bind ViewModel.Modules}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="280" MinItemHeight="160"
                                       ItemsStretch="Fill" MinColumnSpacing="12" MinRowSpacing="12"/>
                </ItemsRepeater.Layout>
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="vm:ModuleItemViewModel">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                CornerRadius="8" Padding="16"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1">
                            <StackPanel Spacing="8">
                                <FontIcon Glyph="{x:Bind IconGlyph}" FontSize="32"
                                          Foreground="{ThemeResource SystemAccentColor}"/>
                                <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" FontSize="15"
                                           TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Description}" FontSize="12"
                                           TextWrapping="Wrap" Opacity="0.7"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <!-- Pasek statusu -->
        <Border Grid.Row="2" Background="{ThemeResource SystemFillColorNeutralBackgroundBrush}" Padding="16,8">
            <TextBlock x:Name="StatusText" Text="Gotowy" FontSize="12" Opacity="0.7"/>
        </Border>
    </Grid>
</Window>
